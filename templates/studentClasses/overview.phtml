<h2>Klasa {$class->symbol}</h2>
<table>
	<tbody>
		<tr>
			<td>Wychowawca</td>
			<td>{$class->leadingTeacher()}</td>
		</tr>
		<tr>
			<td>Rok</td>
			<td>{$class->rok}</td>
		</tr>
		<tr>
			<td>Nazwa</td>
			<td>{$class->nazwa}</td>
		</tr>
		<tr>
			<td>Liczba uczniów</td>
			<td>{count($students)}</td>
		</tr>
	</tbody>
</table>

{if count($students)}
	<table>
		<thead>
			<tr>
				<th>Numer</th>
				<th>Imie</th>
				<th>Nazwisko</th>
				<th>PESEL</th>
				<th>Zdjęcie</th>
			</tr>
		</thead>
		<tbody>
			{foreach $students as $student}
				<tr>
					<td>{$student->nr_dziennika}</td>
					<td>{$student->imie}</td>
					<td>{$student->nazwisko}</td>
					<td>{$student->pesel}</td>
					<td>{$student->photo}</td>
				</tr>
			{/foreach}
		</tbody>
	</table>
{else}
	nie dodano jeszcze uczniów
{/if}
