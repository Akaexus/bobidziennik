<h2>Lista klas</h2>
{if $classes}
    <ul>
        {foreach $classes as $class}
          <li>
        <a href="?s=timetable&class={$class->id}">
          {$class->name()}
        </a>
      </li>
        {/foreach}
    </ul>
{/if}
